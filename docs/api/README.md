# API Specs

This folder is reserved for **machine-readable interface specifications**:

- [`openapi.yaml`](openapi.yaml) — REST API (Swagger UI served by the running server at `/docs`)
- [`asyncapi.yaml`](asyncapi.yaml) — event contracts

## Historical reviews

Historical `/review` outputs and other narrative review artifacts are archived under:

- [`docs/archive/reviews/`](../archive/reviews/)

They are preserved for traceability but **may be stale** vs the current implementation.

