# /sync-contracts

Detect and resolve contract drift between JSON Schemas and API documentation.

**Source of truth:** `.cursor/skills/sync-contracts/SKILL.md` (workflow is maintained there to avoid duplication).

## Usage

Check for drift:
```
/sync-contracts
```

## Instructions

When the user invokes `/sync-contracts`, follow `.cursor/skills/sync-contracts/SKILL.md`.
